{
  "name": "viberevert",
  "version": "0.8.0-beta.0",
  "description": "Deterministic risk checks and session recovery for AI coding agents.",
  "mcpServers": {
    "viberevert": {
      "command": "npx",
      "args": ["-y", "viberevert@0.8.0-beta.0", "mcp", "serve"],
      "cwd": "${workspacePath}"
    }
  }
}
