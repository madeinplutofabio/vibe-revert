M RP handoff named adapters + installers + GitHub Action template (which IS the final G1b scope). Reconciliation note for explicit traceability: earlier G1b drafts mis-shaped the package graph (cycle), proposed `--global` without solving MCP runtime identity, kept NoopPlan in adapters (forces integrations.json read in adapters), missed install lock + recovery journal + collision-proof backup naming + canonical-JSON for SHA + GH Action least-privilege + Windows chmod no-op + recordKey enum + gitignore warning. Final G1b plan includes all corrections. No M RP language is being rewritten.
